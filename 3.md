# 方向 B:从演示品到真工具 —— 任务规划(3.md)

> 2026-08-24 立项。前置结论:V2/V2.1 规格已完成并经真实飞书 E2E 验证;
> 本文档定义后续演进方向、pi 参考项目的学习成果与任务分解。

---

## 一、方向决议

- **选 B:变成真工具**。系统已在真飞书群运行(张三报修 / 于松泽处理),
  从"面试展示品"转向服务真实用户。
- 治理变更:Multi-Agent 已解禁(AGENTS.md「Agent topology」章节),
  但单受限循环仍是默认;任何新 Agent 遵守 Invariant 4(advice-only)
  且单一职责 + 独立测试 + 不共享可变内存。

## 二、语料事实修正

- legacy `storage/tickets.db` 的 359 条工单是**开发调试数据**
  (大量「测试123」「新问题」),内容不可信,不可作训练语料。
- 可用的部分:**结构**——事件序列、状态迁移、协作语义
  (`real_lifecycle_50.db` 是整理过的回放集)。
- 真实语料从现在开始积累:系统已落库每条真群消息,
  持续收集张三/于松泽的真实表达。

## 三、pi(earendil-works/pi)学习成果

参考库已克隆至 `reference/pi`(只读,gitignore 内)。

### 值得学的四个设计(源码证据)

1. **Agent 循环:事件流 + 中途纠偏**
   `packages/agent/src/agent-loop.ts:160-240`
   外循环等 follow-up × 内循环工具批处理;全程类型化事件
   (turn_start/message_start/turn_end/agent_end);
   `getSteeringMessages()` 允许用户在推理进行中注入纠正消息。
   → 我们的多轮澄清场景直接受益("等等,是 A3 不是 A5")。

2. **截断安全**
   `agent-loop.ts:206-212`:`stopReason === "length"` 时该消息所有
   工具调用全部作废("instead of executing potentially borked calls")。
   被截断的 JSON 参数可能解析成功但语义残缺。

3. **Provider 层**(`packages/ai/src/`)
   一个协议一个适配器(openai-completions / anthropic-messages /
   google-generative-ai / bedrock…)+ `.lazy.ts` 延迟加载;
   `env-api-keys.ts` 每 provider 一条环境变量凭据解析链;
   目录只收支持工具调用的模型。
   对照:`app/infrastructure/llm.py` 仅 60 行、硬绑 OpenRouter、
   无重试链无降级切换——最值得照抄的一层。

4. **会话树 + 压缩**(`docs/session-format.md`)
   JSONL 条目 `id/parentId` 成树;compactionSummary 记录
   firstKeptEntryId(保留点)。我们的 recent-6+持久 summary 是其简化版。

### 不引入的部分

- TS 运行时依赖(跨语言无收益);MCP/子代理生态(治理红线仍在)。

## 四、任务清单(B1~B6)

| # | 任务 | 学自 | 规模 |
|---|---|---|---|
| B1 | `llm.py` 重构:Provider 注册表 + 环境变量凭据链 + 失败自动降级次选模型 | pi-ai | 小 |
| B2 | 意图路由改造:关键词表 → 小模型分类,关键词表降级兜底 | — | 中 |
| B3 | RAG 作答门槛提高(0.2→0.35 试跑)+ 低分诚实转人工 | AC-21 既有机制 | 小 |
| B4 | 提示词按受众/场景拆分注册(决策与措辞分离) | legacy prompts + pi skills | 中 |
| B5 | Agent 循环加 steering 注入 + stopReason=length 防御 | agent-loop.ts | 中 |
| B6 | 真实语料持续积累(已自动落库,定期复盘调参) | — | 持续 |

执行顺序建议:B1 → B3 → B2 → B4 → B5(B6 并行)。

## 五、已知遗留(非阻塞)

- SLA 超时引擎未移植(legacy 有);reopen 不支持(设计决定);
  assign/reassign/priority 等辅助命令未移植;
  卡片按钮/线程绑定操作未来工作;
- WeCom GROUP_INBOUND = PENDING_OFFICIAL_SPEC。

---

## 六、JD 市场分析(2026-08 调研)

数据源:CSDN《101 份 AI Agent 岗位 JD 分析》(2025.11)+ BOSS 直聘在售岗位
(Agent应用开发工程师/大模型算法工程师(RAG&Agent)/LangChain工程师等)。

### 高频技能要求(按出现频次)

| 技能 | 频次 | 本项目现状 |
|---|---|---|
| Python | 125 | ✅ 主语言 |
| RAG + 向量数据库 | 123 | ⚠️ 有 RAG 但是 TF-IDF 词频打分,**无向量库、无 embedding** |
| LangChain/LangGraph | 最高频框架,"熟练/精通" | ❌ 自研 workflow,无框架——**最大简历关键词缺口** |
| 多智能体协作 | 明显上升趋势 | 治理已解禁,未有实现 |
| RAG 细化:rerank / 知识更新 / 多模态 | 新增要求 | ❌ 无 rerank |
| 机器学习基础 | 92(了解即可) | — |
| 容器化 | 55 | ⚠️ 有 Dockerfile/docker-compose 但非完整编排 |
| 微服务 | 42 | 单体(可接受,应用开发岗不强制) |
| 云平台 | 38 | 阿里云有现成机器 |
| CI/CD | 28 | ❌ 无 |

### 参照的真实企业级开源项目

| 项目 | 技术栈 | 值得抄的点 |
|---|---|---|
| baibai-awd/customer-support-agent | FastAPI+LangGraph | `classify→retrieve→draft→evaluate` 图编排;**业务回写**;**量化指标**(自动处理率/节省工时/转人工率);SSE 流式;人工修改反馈沉淀评估集 |
| empireaaron/order_agent (TicketBot) | FastAPI+LangChain+LangGraph+Milvus | RBAC、WebSocket 实时、P50/P95/P99 监控看板、意图识别准确率统计 |
| huabeitech/agent-desk | Go+Next.js | **AnswerabilityGate**(知识片段是否足以支撑回答的判定门)、会话到工单闭环、Docker 一键部署 |
| langchain-ai/langgraph (40k★) | 编排框架 | durable execution / HITL interrupt / memory 抽象 |

## 七、最小闭环计划(JD 达标版)

原则:不推翻现有架构(治理层已是差异化优势),把 JD 关键词逐个变成
真实可运行的能力,每个都闭环(功能+测试+文档)。

| # | 任务 | 对应 JD 关键词 | 规模 |
|---|---|---|---|
| C1 | 用 LangGraph 重排现有管线:`classify→retrieve→draft→evaluate` 四节点图,节点=现有服务(intent_router/retriever/support_agent/workflow),状态=AgentContext | LangChain/LangGraph | 中 |
| C2 | 真向量 RAG:embedding(Qwen/OpenAI 兼容接口或本地 bge)+ Chroma/FAISS 本地库 + 知识库导入脚本;TF-IDF 降级保留 | RAG+向量数据库 | 中 |
| C3 | 指标看板 API:`GET /api/dashboard`(分类准确率/自动处理率/转人工率/平均节省工时),数据从 trace/outbox 聚合 | 业务量化(面试杀手锏) | 小 |
| C4 | docker-compose 完整编排(app+向量库+.env 样例)+ README 部署章节 | 容器化 | 小 |
| C5 | SSE 流式回复端点(`/webhooks` 不变,新增 `/api/chat/stream`) | 流式体验 | 小 |
| C6 | 反馈闭环:`POST /api/feedback`(人工改写回复入库)→ 沉淀评估集 | RAG 知识更新 | 小 |

执行顺序:C1(C2 并行)→ C3 → C4 → C5/C6。
完成后本项目同时具备:**企业级治理(独有优势)+ LangGraph 向量 RAG(JD 全关键词)**。

---

## 八、2026-08 最新 JD 信号(更新调研)

数据源:腾讯招聘(混元 Agent 系列岗,页面更新 2026-07-29 ~ 08-21)、
BOSS 在售岗位、《2026年版 AI应用开发工程师入门指南》(2026-05)、西安智能体开发岗(2026-04)。

### 与 2025.11 相比的变化

| 变化 | 证据 | 对本项目 |
|---|---|---|
| **Agent 可观测性/评估成为独立岗位方向** | 腾讯「混元 AI Agent Harness Engineer」(08-21):全链路 tracing & observability、自动化 eval pipeline、A/B testing、regression detection | ⭐ 我们已有 case trace + Agent eval 雏形(AC-A20),**这是最该显性化的既有优势** |
| **MCP 列入核心技术清单** | 2026 入门指南将其与 Function Call 并列 | 治理仍禁;是否解禁待 owner 决定 |
| LangSmith/LangFuse 成"企业必备" | 同上指南 | C 计划应补:trace 输出对齐 OpenTelemetry/LangFuse 语义 |
| 多模态落地必备 | 同上 | 超出最小闭环,列为可选 |
| 私有化部署需求激增(vLLM/ollama) | 同上 | 可选加分项 |
| 国产模型场景(Qwen3/DeepSeek) | 西安 JD、入门指南 | llm.py provider 重构(B1)天然支持 |
| 上下文工程被点名 | 社区概念进入 JD | 我们的两阶段事务+ContextBuilder 即是实践 |
| 不变的基本盘 | Python/FastAPI、LangChain/LangGraph、RAG+向量库、记忆管理、Docker/K8s | C1~C6 计划不变 |

### C 计划修订(并入 2026-08 信号)

- **C3 升级**:指标看板之外,trace 事件对齐标准语义(OpenTelemetry span 形状),
  提供 `GET /api/traces/{id}` 结构化导出 —— 直接对标腾讯 Harness Engineer 岗位职责;
- **C2 选型**:向量库首选 Chroma(本地零依赖,JD 清单内);Milvus 留作部署升级项;
- **新增 C7(可选)**:MCP server 只读暴露(case 查询/工单统计)——需 owner 先解禁治理条目;
- **新增 C8(可选)**:vLLM/Ollama 本地模型接入(B1 完成后即插即用)。

---

## 九、Agent 开发高频关键词 × 治理限制 对照(最终分析)

数据源交叉:kamacoder《Agent大厂面试题汇总》(2026-06)、腾讯 Harness
Engineering JD(2026-08-03)、2026 就业课程大纲、智联大模型应用工程师 JD。

### 高频关键词 Top15(按出现强度)

| # | 关键词 | 热度来源 | 本项目状态 |
|---|---|---|---|
| 1 | RAG(+rerank/混合检索) | 全部来源,缺口最大方向 | ⚠️ TF-IDF,C2 补向量+rerank |
| 2 | Function Calling | 面试必问,"Agent 的基石" | ⚠️ 有只读工具但未走 FC schema 协议 |
| 3 | **MCP** | 面试必问+JD 核心清单,"解决什么问题"是标准题 | ❌ **受限** |
| 4 | ReAct/Plan-and-Execute | 四种工作模式必考 | ⚠️ 自研受限循环,无标准范式命名 |
| 5 | LangChain/**LangGraph** | 框架第一梯队 | ❌ 未用,C1 解决 |
| 6 | **Multi-Agent**(LangGraph/CrewAI/AutoGen) | 大厂必考协作架构 | ✅ 已解禁未实现 |
| 7 | 记忆系统(短期/长期) | 面试高频设计题 | ✅ 已实现且经真实验证 |
| 8 | 安全:HITL/最小权限/幂等/降级 | 面试高频"生产踩坑"题 | ✅ **全部有真实实现** |
| 9 | 可观测:Trace/Token监控/评估体系 | 腾讯专职岗位方向 | ✅ 雏形,C3 显性化 |
| 10 | 向量库 Chroma/Milvus/FAISS | RAG 标配 | ❌ C2 解决 |
| 11 | Skills/提示词工程 | 三层分工论(FC/MCP/Skills) | ⚠️ PromptRegistry 单提示词 |
| 12 | Docker/K8s 部署 | 工程化基本盘 | ⚠️ Docker 有,K8s **受限** |
| 13 | vLLM/Ollama 私有化 | 需求激增 | ❌ B1 后可插 |
| 14 | A2A 协议 | 新兴(MCP 之后) | 不受限(属多Agent范畴,已随解禁放开) |
| 15 | 微调 LoRA/QLoRA | 高端岗(60K+)必需 | 超出应用开发范围,不做 |

### 治理限制解除建议

| 禁令条目 | 建议 | 理由 |
|---|---|---|
| **MCP** | ✅ **建议解除**(条件式:只读 MCP server 先行) | 2026 面试必问+JD 核心技术;我们的只读工具天然适合暴露为 MCP resources/tools;Invariant 4 不受影响 |
| **Kubernetes** | ⚠️ 半解除(允许 K8s manifest 编写/部署,不强制迁移) | 部署类 JD 要求;单机 home-lab 用 compose,K8s 作为简历能力 |
| **GraphRAG** | ❌ 维持禁令 | JD 出现频率低;rerank/混合检索性价比远高于图检索 |
| **Kafka** | ❌ 维持禁令 | JD 几乎不点名;事务 outbox 已覆盖同等语义 |
| ERPNext/十系统/legacy兼容API/多Ticket API | ❌ 维持 | 与 Agent 岗位无关,是历史教训护栏 |

注:Anthropic 官方提醒(面试考点)——"不要过早引入 Multi-Agent,
一个强大的单 Agent 往往比多个简单 Agent 协作更稳定省钱"——与我们
AGENTS.md「单循环默认」的拓扑立场一致,可作为面试观点输出。

### 我们已满足的面试高频考点(简历可直接引用)

幂等性(idempotency store + 并发测试)、HITL 审批(CAS exactly-once)、
超时控制(LLM 15s + 降级)、失败重试(outbox ≤3)、全链路 Trace(actor/
事件/通知)、两阶段事务(LLM 不占写锁)、崩溃恢复(inbound_processing
状态机)、协议诚实(PENDING_OFFICIAL_SPEC)。

---

## 十、定位词:Harness Engineering(缰绳工程)

2026 最热概念:LLM 是"没有缰绳的马",Harness 是套在模型上的工程约束层
(工具权限/事务边界/HITL/超时降级/幂等重试/Tracing/Eval/崩溃恢复)。
证据:腾讯「混元 AI Agent Harness Engineer」岗(08-21)、pi 自我定位
"Pi Agent Harness"、面试题汇总设 Harness Engineering 分册。

**本项目的治理层就是一个完整 Harness 实践**:两阶段事务(LLM 不占写锁)、
advice-only 不变量、Policy 门禁、HITL exactly-once、幂等领取、outbox 重试、
inbound_processing 崩溃恢复状态机、case trace。

简历定位改写:「基于 Harness Engineering 理念构建的生产级支持 Agent:
受限推理循环 + 确定性执行域 + HITL 审批链 + 全链路可观测,
真实飞书环境运行验证。」C 计划 = 给缰绳补马力;差异化 = 缰绳本身。

## 十一、Top14 关键词逐项对照(概念 × 本项目现状)

| # | 关键词 | 概念一句话 | 本项目现状 |
|---|---|---|---|
| 1 | RAG(+rerank/混合检索) | 检索外部知识注入生成,防幻觉;rerank 对召回重排序 | ✅ C2 已落地(2026-08-25):TF-IDF+Qwen3-Embedding(4096维) RRF 双路融合+Qwen3-Reranker 重排;419 条中文语料全量索引;grounding 门控(Invariant 7)保留在关键词侧 |
| 2 | Function Calling | LLM 输出结构化工具调用指令(schema),应用执行后回传 | ✅ C1 已落地(2026-08-25):TOOL_JSON_SCHEMAS 标准 OpenAI FC 格式 ×8 工具(enum/required/description);运行时白名单仍为执法点 |
| 3 | MCP | 工具接入的开放标准协议(Anthropic 2024 开源),解决集成碎片化 | ✅ C7 已落地(2026-08-25):mcp v2.1.0 只读 server(get_case/ticket_stats/search_knowledge + kb://catalog resource),stdio 入口 Claude Desktop 可直连;无任何写面(Invariant 4 上链) |
| 4 | ReAct / Plan-and-Execute | 推理-行动交替循环 / 先规划再分步执行两种工作模式 | ✅ C1 已落地(2026-08-25):classify→retrieve→draft→evaluate 四节点 StateGraph(langgraph 1.2.11 typed state),零行为变更同降级规则 |
| 5 | LangChain / LangGraph | LLM 应用组件库 / 图结构状态机编排框架 | ✅ C1 已落地(2026-08-25):langgraph>=1.2,<2 主依赖;四节点图 + 条件 handoff 边,测试 8 例 |
| 6 | Multi-Agent | 多个专业 Agent 协作(编排者+执行者) | ✅ C10 问数子代理已落地(2026-08-25):NL→受限统计规格→只读执行→口径明确答案;LLM 输出经规则信号校验,畸形输出降级规则解析;单一职责/无状态/无共享内存/只读,符合 AGENTS.md 多 Agent 硬条件 |
| 7 | 记忆系统 | 短期(会话上下文)+长期(跨会话持久)双层 | ✅ 已实现:recent-6+持久工单摘要(短)/CLOSED 后 MemoryExtractor 抽取+recall 召回(长),真实环境验证;**2026-08-26 增强(#6 落地)**:会话级滚动摘要(pi compaction 式,first_kept_message_id 切点不拆轮次,LLM 摘要失败降级确定性抽取)+记忆来源标记 confirmed_closure/force_closed(v2.md §49,召回时 confirmed 打 1.0/force_closed 打 0.85) |
| 8 | 安全(HITL/最小权限/幂等/降级) | 高危操作人工审批;最小授权;重复执行一次生效;失败有退路 | ✅ **全部有真实实现**:Approval CAS exactly-once、只读工具白名单、幂等领取+并发测试、LLM 超时→确定性降级；C9 新增意图级最小授权(闲聊轮零检索面)+L4 实体拦截层(PII 禁入向量检索+输出脱敏) |
| 9 | 可观测(Trace/Eval) | 全链路追踪+质量评估管线 | ✅ **C3 已落地(2026-08-25)**:run_evals.py 一键回归(RAG 14例 Recall@3=100% MRR=1.0 / 混合检索 rerank 实测 avg_top1=0.977 / Agent 黄金集 pytest 门控)+ trace JSONL 标准化导出 + 报告自动生成(md+json) |
| 10 | 向量库(Chroma/Milvus/FAISS) | embedding 语义检索存储 | ✅ NumpyVectorStore 默认后端(零依赖)+ChromaVectorStore 可选后端(KB_VECTOR_BACKEND=chroma),统一 VectorStore 接口(2026-08-25) |
| 11 | Skills/提示词工程 | 领域知识编码为可复用能力包;按场景拆提示词 | ✅ **B4-v2 已落地(2026-08-25)**:Skill 注册表系统——5 技能七字段 meta(applies_to/not_applies_to 负样本/requires/failure_mode/output_format)+ select() 按意图路由 + 摘要/全文两级加载(决策轮 summary、执行轮 full)+ 注入量入 trace;闲聊轮注入降 **75.6%**(0.244 比率,测试断言);损坏回退主流程(prompt_error 兜底) |
| 12 | Docker/K8s | 容器化交付与编排部署 | ⚠️ Dockerfile/compose 有但未成体系 → C4 完整编排(+K8s manifest 已解禁) |
| 13 | vLLM/Ollama 私有化 | 本地/私有推理服务 | ⚠️ B1 provider 注册表已落地(2026-08-25):openrouter/bailian/deepseek 内置+通用 env 解析+FallbackLLMClient,Ollama/vLLM 即插即用;本地推理实测待做 → C8 |
| 14 | A2A 协议 | Agent 间协作通信标准(Google 推出,与 MCP 互补) | 未涉及;随 C10 多 Agent 落地后评估 |

结论(2026-08-25 更新):**13 项已有真实现**;B1/C9/C2/C10/C1/C3/C7/B4 已完成,C8(Ollama 实测)owner 取消、C4(owner 暂停)、A2A 条件触发。缺口均有明确任务归属。

## 十二、全市场收敛分析:五模块 × 组件排行对照(2026-08-26 调研)

> 数据源:全市场 JD 收敛调研——职责五模块(JD 骨架,面试官默认的"干活大纲")
> 与组件排行(JD 明确要求"会设计/会调优"的内部零件,深挖考点)。
> 用法:简历按组件排行逐行自查;面试五模块讲故事、组件排行备深挖。

### 12.1 职责五模块 × 本项目

| # | 模块 | 典型 JD 原文 | 现状 | 缺口 |
|---|---|---|---|---|
| 1 | 全链路架构 | 任务规划/记忆管理/工具调用/多智能体协同核心模块 | ✅ 四件套齐:受限循环+LangGraph 图(规划)、双层记忆、FC×8 工具、问数子代理+A2A | — |
| 2 | 框架编排 | LangChain/LangGraph 落地 ReAct/CoT 推理范式 | ✅ langgraph 1.2.11 四节点图+条件 handoff 边+降级路径;叙事="框架是壳,Harness 是核" | — |
| 3 | RAG 全流程 | 文档解析→向量分片→Embedding检索→重排,降低幻觉 | ✅ 双路 RRF 融合+Qwen3-Reranker+AnswerabilityGate,Recall@3=100%(2026-08-25 实测) | 分片策略无故事(FAQ 短文档直入库,"解析→分片"环节讲不出设计权衡) |
| 4 | 生产工程化 | 限流熔断降级、异步流式、异常重试、权限隔离 | ✅ 幂等领取/HITL CAS/LLM 超时降级/outbox 重试≤3/崩溃恢复/L4 PII 脱敏/意图级最小授权 | ❌ **SSE 异步流式(C5)未做**——JD 原文明写词,五模块中唯一空白子项 |
| 5 | 前沿预研 | MCP/A2A/本地推理 POC 验证 | ✅ MCP 只读 server 已落地(v2.1.0);A2A agent card+JSON-RPC 已超额落地(ff82d12) | ⚠️ Ollama 实测被 owner 取消(C8),但 provider 注册表就绪,实测仅差半小时 |

### 12.2 组件排行 × 本项目(深挖考点自查)

| 排名 | 组件 | 提及率(高薪组) | 现状 | 面试深挖风险 |
|---|---|---|---|---|
| 🥇 | 工具生态 ToolUse | 49% | ✅ TOOL_JSON_SCHEMAS 标准协议+意图级白名单+≤2 次调用上限 | 工具调用审计日志未入 trace(#7 补齐项遗留),"误调用怎么排查"缺 trace 证据 |
| 🥈 | **记忆系统 Memory** | 45%(**65%**) | ⚠️ 双层有且真实验证(recent-6+持久摘要/CLOSED 后抽取+recall);**无会话压缩、无来源标记**(升级计划 #6 未落地) | **高危**:65% 高薪组必追问"上下文超长怎么办",目前只能答 recent-6 截断,答不出 pi 式 compaction(first_kept_message_id) |
| 🥉 | 任务规划 Planning | 42% | ✅ 反主流叙事:受限循环优于自由规划(Anthropic 同款观点,Harness 立场) | 低 |
| 4 | 上下文管理 | 42% | ✅ ContextBuilder 角色隔离/可信度分层 + Skills 两级加载,**闲聊轮注入降 75.6%**(测试断言可复查) | 低——现成最佳故事 |
| 5 | **评测体系 Eval** | 41%(**52%**)⭐ | ✅ run_evals.py 标准化 runner+黄金集 pytest 门控+报告自动生成(md+json) | 中:zh_golden 500 条中文意图基准未生成(§5.2 方案搁置),"分类准确率"无基准数据 |
| 6 | 可观测 Tracing | 16% | ✅ OT 形状 JSONL 导出+/traces/{trace_id} 结构化端点 | 低 |
| 7 | 安全护栏 | 16% | ✅ 项目本体即 Harness(幂等/HITL/最小授权/降级全真实实现) | 低,差异化优势 |
| 8 | 反思 Reflection | 8% | ❌ evaluate 节点只是校验非反思 | 可不补;"evaluate+LLM 输出规则交叉校验=轻量反思形态"可作观点输出 |

### 12.3 补强优先级(修正外部清单后定稿)

外部建议为"上下文压缩 > MCP server > Reflection"。修正:**MCP server 已完成
(853f33a),该条作废**,空出名额给 SSE 流式:

| 优先级 | 动作 | 理由 | 成本 |
|---|---|---|---|
| ~~P1~~ ✅ | 会话级滚动摘要(pi compaction 式,#6):超 N 条压缩为 summary entry+first_kept_message_id;记忆写入加 confirmed_closure/force_closed 来源标记 | **已完成(2026-08-26)**:SessionCompactor+session_compactions 表(migration 0015)+ContextBuilder history_summary+5 模板注入;回归测试 10 例,全量 361 绿 | 小 |
| ~~P2~~ ✅ | SSE 流式端点(C5):`GET /api/chat/stream` | **已完成(2026-08-26)**:SSE 分阶段事件流(received→prepared→agent→completed+done);诚实流式立场——决策 JSON 是单一 schema 契约不拆 token(与 B5 截断防御同源),流的是管线进度;新增 api 渠道适配器,两阶段管线加 on_stage 观测回调(异常不影响主流程);webhooks 不变;4 例测试+真机 curl 冒烟,全量 365 绿 | 小 |
| P3 | zh_golden 500 条意图评测集生成(scripts/localize_dataset.py 已就绪,CLINC150→本项目意图体系中文改写) | Eval(52%) 从"有 runner"升级为"有中文基准+错误率对比数据" | 小 |
| P4 | Ollama 本地推理 30 分钟实测(争取 owner 解冻 C8) | JD 骨架第 5 模块原文"本地推理 POC";`LLM_PROVIDER=ollama` 即插即用 | 极小 |
| 不做 | Reflection | 8% 提及率,性价比最低 | — |

结论:前 5 组件形式上全覆盖(够到高薪组门槛线),但 **Memory/Eval 两个高薪
翻倍项均为"有骨架、缺纵深"**——P1~P3 全是小改动,完成后从"够到"变"站稳"。
