可以。现在已经不是“继续讨论 V2 要不要做什么”的阶段了，这次可以给 Codex 一个**正式的 V2 实现任务**。

我会把它定义成：**不推翻 V1，而是在 V1 已经验证过的 Core 上，一次性补齐 Collaboration / Conversation / Delivery / Operator / Closure / HITL / Reliability。**

这样做的底气是 V1 已经有一个相当稳定的底座：Canonical Identity、跨渠道 Ticket Resolution、四状态 Ticket + Event 同事务、RAG grounding、Agent advice-only、Long-term Memory 都已落地，而且当前审计实际跑出了 `139 passed`。 同时审计已经实证了 V2 要解决的问题，包括无 Conversation Purpose、无 Operator identity、无 outbound、无 requester confirmation，以及并发 webhook / claim 的竞态。

渠道协议方面，我会在 Prompt 里加一条非常强的约束：**Mock 网络，但不能 Mock 协议。**例如飞书官方当前明确要求消息事件幂等优先使用 `message_id`，支持单聊以及群聊 @ 机器人等消息事件；发送 API 可以使用 `open_id` 或 `chat_id` 等作为接收目标。([飞书开放平台][1])

企业微信这边，我这次搜索没有稳定检索到 `developer.work.weixin.qq.com` 上足以覆盖我们所有群入站场景的官方页面，所以我会要求 Codex **只能依据它实际找到并记录下来的企业微信官方文档实现 Protocol Adapter；无法用官方文档证明的能力必须标为 `UNSUPPORTED/PENDING_OFFICIAL_SPEC`，绝对不能根据 Legacy 或第三方文章伪造成官方能力。**

下面这整段可以直接给 Codex / Claude Code。

---

# Support Agent Lite V2 — Full Collaboration Layer Implementation

## 任务性质

你现在开始正式实现：

```text
Support Agent Lite V2
```

这次不是架构审计。

这是一个：

```text
INSPECT
→ DESIGN AGAINST EXISTING V1
→ IMPLEMENT
→ MIGRATE
→ TEST
→ DOCUMENT
→ VERIFY
```

的完整开发任务。

允许修改 V1 业务代码、数据库迁移、测试和文档。

但是：

> **禁止推翻 V1 已经验证成立的核心架构。**

V2 的目标不是重写项目。

V2 的目标是：

> **在 V1 的 AI Support Core 上补齐真实企业协作所需的 Conversation / Role / Operator / Notification / Outbound / Confirmation / HITL Execution / Reliability Layer。**

---

# 1. 开始前必须读取

首先读取：

```text
AGENTS.md

README.md

docs/PRODUCT_SCOPE.md
docs/ARCHITECTURE.md
docs/DOMAIN_MODEL.md
docs/GOLDEN_PATH.md
docs/ACCEPTANCE_TESTS.md
docs/DEVELOPMENT_PLAN.md
docs/IMPLEMENTATION_BACKLOG.md
docs/LEGACY_PORT_MAP.md
docs/HANDOVER.md

V1_TO_V2_ARCHITECTURE_AUDIT.md
```

以及：

```text
app/
storage/migrations/
tests/
seed/
```

Legacy 只读参考位于：

```text
reference/
```

优先参考：

```text
reference/scripts/wecom_bridge_server.py

reference/workflows/case_collab_workflow.py

reference/workflows/support_intake_workflow.py

reference/channel_adapters/wecom_adapter/adapter.py

reference/channel_adapters/feishu_adapter/adapter.py

reference/core/hitl/

reference/docs/upgrade5-wecom-dispatch.md
reference/docs/upgrade5-agents-ops-dispatch.md

reference/升级5-1.md
```

Legacy 只能用于理解：

```text
业务语义
消息文案
协作流程
历史问题
```

不要复制它的整体架构。

---

# 2. V1 基线

开始实现前先运行：

```bash
git status
git branch --show-current
git rev-parse HEAD

pytest -p no:warnings
```

当前已知 V1 基线大约是：

```text
139 tests passed
```

但必须以本次实际运行结果为准。

记录：

```text
branch
commit
test count
working tree state
```

---

# 3. V2 必须保护的 V1 Invariants

以下设计已经由 V1 验证。

**V2 禁止破坏。**

## 3.1 Canonical User

继续保持：

```text
Channel Identity
      ↓
Canonical User
```

禁止恢复：

```text
channel user == business user
```

---

## 3.2 User-centric Ticket Resolution

继续保持：

```text
explicit ticket id
↓
requester session context
↓
canonical user active tickets
↓
0 / 1 / multiple clarification
```

禁止恢复 Legacy：

```text
session.active_ticket_id
```

作为工单解析的核心。

---

## 3.3 Ticket 单状态模型

继续使用：

```text
OPEN
IN_PROGRESS
RESOLVED
CLOSED
```

禁止重新引入：

```text
lifecycle_stage
handoff_state
risk_level
metadata_json business state
```

---

## 3.4 Ticket + TicketEvent 同事务

任何工单状态变化：

```text
Ticket State
+
TicketEvent
```

必须原子提交。

---

## 3.5 Agent advice-only

LLM / Agent 可以：

```text
classify
summarize
suggest priority
suggest category
recommend action
draft reply
```

但不能直接：

```text
claim
resolve
close
escalate
approve
assign
```

真正执行必须经过确定性 Domain Service。

---

## 3.6 Approval 独立于 Ticket

不要把：

```text
PENDING_APPROVAL
```

加入 TicketStatus。

---

## 3.7 RAG Grounding

低可信知识：

```text
不得自由编造
```

---

## 3.8 单一 Ticket API

V2 只能存在：

```text
one TicketService / one lifecycle implementation
```

禁止重新产生：

```text
legacy API
v2 API
lifecycle API
```

多轨。

---

# 4. V2 最终产品定义

V2 最终必须能够表达：

```text
Channel
      ↓
Conversation
      ↓
Conversation Type / Purpose
      ↓
Canonical Actor
      ↓
Role / Authorization
      ↓
Ticket
      ↓
Business Event
      ↓
Audience Policy
      ↓
Notification
      ↓
Channel Outbound Contract
```

---

# 5. Channel 和 Business Role 必须彻底分离

禁止：

```text
WeCom = requester
Feishu = operator
```

正确模型：

```text
WeCom
├── Requester
└── Operator

Feishu
├── Requester
└── Operator
```

具体某个 Channel Connector 是否具有：

```text
GROUP_INBOUND
GROUP_OUTBOUND
DM_INBOUND
DM_OUTBOUND
```

属于：

```text
Channel Capability
```

而不是业务规则。

---

# 6. Conversation 成为正式领域模型

V2 必须新增正式的 Conversation 模型。

至少表达：

```text
id

channel
channel_conversation_id

conversation_type
purpose

queue
location

enabled
```

具体字段可以基于现有代码微调，但不能继续只用一个裸：

```text
conversation_id: str
```

承载所有语义。

---

# 7. ConversationType

至少：

```text
DM
GROUP
```

不要使用：

```text
channel == ...
```

推断 group/dm。

---

# 8. ConversationPurpose

至少：

```text
REQUESTER
OPERATOR
APPROVAL
```

含义：

### REQUESTER

普通员工：

```text
报修
补充
FAQ
查询
确认
```

### OPERATOR

维修人员：

```text
查看工单
认领
解决
升级
内部协作
```

### APPROVAL

审批人员：

```text
approve
reject
```

---

# 9. Conversation Purpose 与 User Role 是两个维度

不要设计：

```text
RequesterUser
OperatorUser
```

两套身份。

继续使用：

```text
Canonical User
```

然后建立 Role / Permission。

例如：

```text
user_001
role = requester
```

```text
user_ops_001
role = operator
queue = facility
```

```text
user_manager_001
role = approver
```

同一个 Canonical User 可以拥有多个 Role。

---

# 10. Operator 必须成为真正的 Canonical Actor

V1 当前 claim 没有真实 operator。

V2 必须解决。

例如：

```text
user_ops_001

channel identities:

wecom: lihua
feishu: ou_lihua
```

无论：

```text
WeCom claim
```

还是：

```text
Feishu claim
```

最终必须得到：

```text
actor_user_id = user_ops_001
```

---

# 11. Ticket 必须支持真实 Assignment

当前：

```text
claim
=
OPEN → IN_PROGRESS
```

是不够的。

V2：

```text
CLAIM T1001
```

必须产生：

```text
assignee_user_id = user_ops_001

OPEN
↓
IN_PROGRESS
```

以及：

```text
TicketEvent
type = ticket_claimed
actor_user_id = user_ops_001
```

---

# 12. Ticket operational context

V1 Agent 已经产生：

```text
summary
category
priority_suggestion
recommended_action
```

但没有真正进入业务 Ticket。

V2 需要合理持久化真正有业务价值的字段，例如：

```text
summary
category
priority
queue
assignee_user_id
source_conversation_id
```

不要无脑把所有 Agent 输出写进 Ticket。

保持：

```text
Agent suggestion
↓
Policy / Workflow
↓
accepted business value
```

---

# 13. Requester Public / Private / Internal 三层可见性

V2 不只有：

```text
Requester
Operator
```

还必须表达：

```text
PUBLIC
PRIVATE
INTERNAL
```

---

## PUBLIC

可以发到 Requester Group：

```text
ticket id
current status
operator display name/team
简短处理结果
```

---

## PRIVATE

只发报修人：

```text
完整问题
用户相关信息
完整处理详情
个性化进展
```

---

## INTERNAL

只允许 Operator / Approver：

```text
Agent diagnostic
SOP
internal note
risk analysis
approval reason
internal recommendation
```

测试必须证明：

```text
INTERNAL
```

不会被 Requester notification 泄漏。

---

# 14. 建单时形成三面协作

Requester 在 Group 创建 Ticket：

```text
@bot A3 空调坏了
```

成功后至少形成三种业务输出。

---

## 14.1 Requester Group Public Receipt

例如：

```text
张三，已受理，工单 T1001 已创建。
详细信息已私发给你。
```

---

## 14.2 Requester Private Detail

DM：

```text
工单：T1001
问题：A3 空调无法制冷
状态：OPEN
优先级：P2
```

后续可以继续私聊补充。

---

## 14.3 Operator Work Item

Operator Conversation：

```text
新工单 T1001

优先级：P2
队列：facility
地点：A3
报修人：张三

摘要：
A3 空调无法制冷

Agent 建议：
...

可执行：
CLAIM T1001
```

三条消息必须来自同一个 Ticket Created Business Event。

---

# 15. Cross-conversation Ticket Continuity

必须支持：

```text
Requester Group
↓
create T1001
```

然后：

```text
Requester DM
↓
“下午三点以后我才在办公室”
```

系统继续：

```text
T1001
```

不得：

```text
create T1002
```

这不是 Cross-channel。

这是：

```text
Cross-conversation continuity
```

---

# 16. Session → Ticket Context 必须持久化

V1：

```python
_session_ticket: dict
```

是进程内存。

V2 不允许关键业务续聊依赖进程内字典。

需要持久化 requester-side Ticket Context。

但是注意：

> 不得重新让 Session 成为 Ticket Resolution 的唯一真相。

User-centric resolver 仍然是主算法。

---

# 17. Operator shared conversation 不允许隐式 active ticket

Operator Group 里可能同时出现：

```text
T1001
T1002
T1003
```

因此禁止：

```text
operator_group.active_ticket
```

以及：

```text
“处理好了”
→ 自动猜最近一张 Ticket
```

Operator action 必须来自：

```text
explicit ticket id
```

或者：

```text
verified card/callback context
```

或者：

```text
reply/thread context explicitly bound to ticket
```

第一版命令可以：

```text
/claim T1001
/resolve T1001 已更换控制板
/escalate T1001 原因
```

但不要复制 Legacy 19 条 command。

---

# 18. Domain Action 与 Slash Command 分离

核心 Domain Actions：

```text
CLAIM
RESOLVE
REQUESTER_CONFIRM
REQUESTER_REJECT_RESOLUTION
ESCALATE
FORCE_CLOSE
APPROVE
REJECT
```

可以按需要保留：

```text
REASSIGN
```

但不要因为 Legacy 有命令就全部 port。

Slash command / card / REST 都只是：

```text
Action Input Adapter
```

最终统一成：

```text
TicketAction
```

---

# 19. Claim 必须原子化

当前 V1 已实测：

```text
两个 concurrent claim
→ 两个都成功
```

V2 必须修复。

要求：

```text
OPEN
+
assignee IS NULL
```

才能 claim。

数据库更新必须使用类似：

```text
UPDATE ...
WHERE
    id = ?
    AND status = 'OPEN'
    AND assignee_user_id IS NULL
```

并检查：

```text
rowcount
```

两个 Operator 同时认领：

```text
exactly one succeeds
```

另一方得到明确：

```text
AlreadyClaimed / Conflict
```

---

# 20. Claim 后不是“简单发两条消息”

需要正式区分：

```text
OPERATOR_ACTION_RECEIPT
```

和：

```text
REQUESTER_STATUS_UPDATE
```

---

## Operator Action Receipt

发送到：

```text
Action Origin Conversation
```

例如：

```text
认领成功：
T1001
当前处理人员：李师傅
```

---

## Requester Lifecycle Update

发送到：

```text
Requester notification target
```

例如：

```text
工单 T1001 已由李师傅接手处理。
```

两条消息源自同一个：

```text
ticket_claimed
```

业务事件。

---

# 21. Notification 必须成为独立层

禁止在 `SupportWorkflow` 里大量：

```python
if claimed:
    send(...)
if resolved:
    send(...)
```

需要形成类似：

```text
Business Event
      ↓
Notification Policy
      ↓
Notification Intent
      ↓
Audience / Visibility
      ↓
Target Resolver
      ↓
Outbound Message
      ↓
Channel Transport
```

---

# 22. Notification Type

至少考虑：

```text
REACTIVE_REPLY

PRIVATE_DETAIL

REQUESTER_STATUS_UPDATE

OPERATOR_WORK_ITEM

OPERATOR_ACTION_RECEIPT

REQUESTER_CONFIRMATION_REQUEST

APPROVAL_REQUEST

APPROVAL_RESULT
```

具体 enum 名称可调整。

核心是：

> Notification Type 表达“为什么发”，不是“发到哪个平台”。

---

# 23. Notification Target Resolution

目标解析不能继续使用：

```text
ticket.session_id
```

Ticket 可能涉及：

```text
source requester group
requester DM
operator group
approver DM
later Feishu DM
```

需要正式的 Target Resolver。

支持：

```text
Requester Public Conversation

Requester Private Conversation

Primary Operator Conversation

Action Origin Conversation

Approver Conversation
```

---

# 24. 不要默认所有渠道广播

如果用户：

```text
WeCom 创建 T1001
```

后来：

```text
Feishu 查询 T1001
```

不能因为这个用户绑定了两个渠道，就：

```text
WeCom + Feishu
全部同时通知
```

Notification 应该有明确的：

```text
preferred / active notification target
```

避免重复通知。

---

# 25. Notification Outbox

V2 增加 Outbound 后必须处理可靠性。

建议使用：

```text
Transactional Outbox
```

核心要求：

```text
Ticket change
+
TicketEvent
+
Notification Outbox record
```

必须在同一个数据库事务里产生。

真正 Channel send 可以在事务之后执行。

这样即使 Channel delivery 失败：

```text
business event
```

不会丢。

---

# 26. Notification 幂等

不要沿用 Legacy：

```text
同群 + 同单 + 同文本 + 30 秒
```

作为主要机制。

用业务身份去重。

例如：

```text
source_event_id
+
notification_type
+
target
```

必须唯一。

同一个：

```text
ticket_claimed
```

不能给同一 Target 生成两次相同生命周期通知。

---

# 27. Outbound Contract

即使 V2 不连接真实互联网，也必须完整实现：

```text
OutboundMessage

DeliveryTarget

DeliveryAttempt

DeliveryResult

ChannelOutboundClient
```

---

# 28. Network 必须 Mock，但 Protocol 不能 Mock

V2 默认不得向：

```text
open.feishu.cn
developer.work.weixin.qq.com / qyapi.weixin.qq.com
```

等真实外部服务发请求。

使用：

```text
FakeTransport
RecordingTransport
MockHttpTransport
```

之类机制。

但是请求结构必须尽量与官方协议一致。

---

# 29. Official-doc-only Rule

渠道协议实现必须严格依据：

```text
官方飞书开放平台文档

官方企业微信开发者文档
```

不得依据：

```text
Legacy assumptions
博客
第三方教程
StackOverflow
搜索摘要
```

决定协议字段。

Legacy 只能用于：

```text
业务语义
```

不能用于：

```text
官方协议真实性
```

---

# 30. 建立 Channel Protocol Evidence 文档

新增：

```text
docs/CHANNEL_PROTOCOL_MATRIX.md
```

每一个实现的渠道能力必须记录：

```text
Channel
Capability
Official documentation title
Official documentation URL
Fields used
Headers used
Idempotency field
Supported?
Implemented?
Simulated?
Real-network enabled?
```

---

# 31. 如果官方文档无法证明某项能力

必须：

```text
UNSUPPORTED
```

或：

```text
PENDING_OFFICIAL_SPEC
```

禁止自己发明。

特别是企业微信：

```text
GROUP_INBOUND
Operator Group Command
```

如果当前使用的官方产品/API 文档不能证明支持：

不要假装支持真实接入。

业务层可以拥有：

```text
OPERATOR Conversation
```

但 Channel Capability 必须诚实标记。

---

# 32. ChannelCapability

V2 建立 Connector Capability 描述。

例如：

```text
DM_INBOUND
DM_OUTBOUND
GROUP_INBOUND
GROUP_OUTBOUND

MESSAGE_REPLY
CARD_ACTION
WEBHOOK_VERIFICATION
```

具体 enum 可调整。

业务层可以查询 capability。

---

# 33. Feishu Protocol

实现前必须重新查阅**当前官方飞书文档**。

至少验证：

```text
im.message.receive_v1

message_id

sender.sender_id.open_id

chat_id

chat_type

URL verification / challenge

Verification Token

Encrypt Key / signature（按官方当前配置方式）

POST /open-apis/im/v1/messages

receive_id_type=open_id

receive_id_type=chat_id

tenant_access_token
```

不要只凭当前知识实现。

---

# 34. Feishu Fixture 必须官方形状

测试 Fixture 必须接近真实官方 Event。

不能用：

```json
{
  "user": "张三",
  "group": "xxx",
  "text": "xxx"
}
```

替代协议测试。

Business-level unit test 可以使用内部对象。

但 Protocol-level integration test 必须使用：

```text
official-shaped payload
```

---

# 35. Feishu Idempotency

接收消息场景：

优先：

```text
message_id
```

不要把：

```text
event_id
```

作为消息业务幂等的第一选择。

---

# 36. Feishu Outbound

构造真实协议兼容请求，但由 FakeTransport 接收。

例如：

### DM

```text
receive_id_type=open_id
```

### Group

```text
receive_id_type=chat_id
```

测试必须验证：

```text
URL
query parameter
Authorization header shape
content-type
request body
```

---

# 37. WeCom Protocol

必须联网查：

```text
developer.work.weixin.qq.com
```

当前官方文档。

记录真实官方文档来源。

至少确认：

```text
callback URL verification

Token

EncodingAESKey

signature / msg_signature

timestamp

nonce

encrypted request

message id

application message outbound

access token

user message target

group/appchat capability
```

但：

> 只实现官方文档明确证明的能力。

---

# 38. Legacy WeCom HMAC 禁止 Port

Legacy：

```text
hmac-sha256(timestamp:nonce)
```

不是 V2 的协议来源。

禁止复制。

---

# 39. Protocol Verification 必须可单测

没有真实 Secret 也要能够：

```text
test token
test encoding key
test verification token
test encrypt key
```

跑官方算法/协议 Fixture。

以后换真实配置：

```text
不改业务代码
不改 parser contract
```

---

# 40. RAG 保持轻量，不做复杂升级

V2 不需要为了版本号引入：

```text
GraphRAG
reranker
hybrid retrieval
vector DB cluster
```

继续保持当前 RAG grounding。

---

# 41. 修复 NO_ANSWER 假转人工

现在：

```text
“未找到资料，已转人工”
```

但实际上没有 Ticket。

V2 禁止这种语义不一致。

如果 policy 是：

```text
NO_ANSWER → human support
```

那么必须真实：

```text
create ticket
↓
operator work item
↓
requester truthful reply
```

否则文案只能说：

```text
“当前知识库没有可靠答案。”
```

不得谎称转人工。

---

# 42. Persisted Ticket Summary / Short-term Memory

V2 完成 Short-term Memory。

不能只：

```text
recent 6
+
实时临时拼 summary
```

需要持久化：

```text
Ticket Summary
```

并在有意义的：

```text
new message
claim
resolve
important update
```

后更新。

---

# 43. Agent Context

V2 的 Agent Context 至少可以包含：

```text
Current Message

Canonical Actor

Actor Role

Conversation Type
Conversation Purpose

Ticket
Ticket Summary

Recent Messages

Relevant Long-term Memories

Relevant RAG Sources
```

但不同 Audience 使用的 Context 要避免内部信息泄漏。

---

# 44. Resolve ≠ Close

继续使用四状态：

```text
OPEN
IN_PROGRESS
RESOLVED
CLOSED
```

但 V2 赋予 `RESOLVED` 真正业务意义：

```text
Operator believes fixed
+
waiting for requester confirmation
```

---

# 45. Normal Closure

正常关闭流程：

```text
IN_PROGRESS
↓
Operator RESOLVE
↓
RESOLVED
↓
Requester Confirmation Request
↓
Requester CONFIRM
↓
CLOSED
↓
Memory Extraction
```

---

# 46. Requester 可以跨渠道确认

例如：

```text
WeCom Group
→ create T1001
```

然后：

```text
Feishu DM
→ “T1001 已经好了”
```

只要：

```text
feishu identity
↓
same canonical user
```

并且 T1001 属于该用户：

允许：

```text
CONFIRM
↓
CLOSED
```

---

# 47. Requester 拒绝解决结果

如果：

```text
RESOLVED
```

用户说：

```text
还没好
```

需要支持：

```text
RESOLVED
↓
IN_PROGRESS
```

产生明确 TicketEvent，例如：

```text
resolution_rejected
```

不要新建 T1002。

---

# 48. Operator Force Close

如果保留 Operator 强制关闭：

必须：

```text
reason required
```

并作为高风险动作处理。

不得与：

```text
Requester Confirm Close
```

语义混淆。

---

# 49. Memory 必须依赖真正 Closure

Long-term Memory 正常情况下只在：

```text
CLOSED
```

后提取。

优先：

```text
Requester-confirmed closure
```

的事实。

防止错误 `RESOLVED` 提前污染长期记忆。

---

# 50. HITL 必须从“审批记录”升级成完整执行链

V1：

```text
Approval APPROVED
→ nothing
```

V2 必须完成：

```text
Action Request
↓
Policy
↓
Approval PENDING
↓
APPROVED
↓
Action Executor
↓
Business Effect
↓
TicketEvent
↓
Notification
```

---

# 51. Pending Action 独立建模

不要把 pending action 塞进：

```text
ticket.metadata
```

需要明确的数据模型，例如：

```text
PendingAction
```

或等价模型。

至少：

```text
ticket_id
action_type
payload
requested_by
approval_id
execution_status
executed_at
```

---

# 52. Approval Policy

至少明确：

```text
ESCALATE
```

属于：

```text
approval required
```

如果：

```text
FORCE_CLOSE
```

存在，也建议属于高风险。

不要让任意字符串都成为可审批 action。

使用 action whitelist / enum。

---

# 53. Approval 执行幂等

即使：

```text
approve endpoint
```

被调用两次：

必须：

```text
business action exactly once
```

Approval 的现有 CAS 安全性需要保持。

---

# 54. Trace 升级为 Case Trace

V1 的 message trace 保留。

V2 再补：

```text
actor_user_id
ticket_id
event_id
notification_id
delivery_attempt_id
approval_id
```

之间的关联。

---

# 55. TicketEvent 增加必要审计上下文

至少应该能够回答：

```text
谁操作？
哪张 Ticket？
什么事件？
由哪个 trace 触发？
```

例如：

```text
actor_user_id
trace_id
```

不应该再出现：

```text
started
```

但不知道谁 started。

---

# 56. V2 Trace 最终必须能回答

```text
为什么 T1001 被创建？

谁 claim？

哪个 Conversation 发起 claim？

为什么通知了这个 Requester？

为什么没有通知另一个渠道？

谁 resolve？

Requester 在哪个渠道确认？

谁批准 escalation？

批准之后执行了什么？

Memory 为什么被写入？
```

---

# 57. Idempotency 必须原子化

当前：

```text
check
↓
business execution
↓
mark
```

有并发窗口。

V2 必须改为真正的：

```text
claim idempotency key
```

原子过程。

同一消息并发：

```text
business effect exactly once
```

---

# 58. Identity 并发必须安全

两个并发首次消息：

```text
same channel
same channel_user_id
```

不得出现：

```text
500 IntegrityError
```

唯一约束冲突必须：

```text
catch
↓
re-read existing identity
```

或使用等价原子方案。

---

# 59. Concurrent Claim

必须增加真实并发测试。

至少：

```text
20 / 50 concurrent claim attempts
```

只允许：

```text
1 winner
```

不要为了简历硬编码数量；测试能稳定跑即可。

---

# 60. Notification 并发 / Retry

FakeTransport 可以模拟：

```text
success
temporary failure
permanent failure
```

V2 至少要验证：

```text
business transaction committed
↓
delivery fails
↓
outbox retained / retryable
```

不要因为一次模拟 HTTP 失败回滚 Ticket。

---

# 61. Demo 不真实联网

V2 默认：

```text
REAL_CHANNEL_NETWORK=false
```

类似机制。

所有测试和 Demo：

```text
不需要真实 credential
不访问真实 Feishu
不访问真实 WeCom
```

---

# 62. 但必须支持未来接入

未来给出：

```text
App ID
Secret
Corp ID
Agent ID
Token
EncodingAESKey
Verification Token
Encrypt Key
real conversation ids
```

以后：

只需要：

```text
config
+
RealHttpTransport
```

不得重新设计：

```text
Identity
Conversation
Ticket
Notification
Workflow
```

---

# 63. Acceptance Cases

V1 AC-01 ~ AC-10 必须全部继续通过。

然后增加 V2 Acceptance。

至少覆盖以下内容。

---

## AC-11 Requester Group Create

官方形状的模拟 Channel event：

```text
Requester Group
“A3 空调坏了”
```

得到：

```text
T1001
```

同时生成：

```text
public requester receipt
private requester detail
operator work item
```

---

## AC-12 Cross-conversation Continuation

Group 创建 T1001。

DM：

```text
“下午三点以后我在办公室”
```

继续 T1001。

---

## AC-13 Canonical Operator

同一维修人员：

```text
wecom identity
feishu identity
```

解析到：

```text
same canonical user
```

---

## AC-14 Operator Claim

```text
operator_001
CLAIM T1001
```

结果：

```text
assignee = operator_001
OPEN → IN_PROGRESS
```

并产生：

```text
operator receipt
requester lifecycle notification
```

---

## AC-15 Concurrent Claim

两个 Operator 同时 claim。

结果：

```text
1 success
1 conflict
```

且：

```text
1 ticket_claimed event
```

---

## AC-16 Shared Operator Conversation

没有显式 Ticket ID：

```text
“处理好了”
```

不得猜 Ticket。

---

## AC-17 Resolve

Operator：

```text
RESOLVE T1001
```

结果：

```text
IN_PROGRESS → RESOLVED
```

Requester 收到：

```text
confirmation request
```

不得立即 CLOSED。

---

## AC-18 Cross-channel Requester Confirmation

WeCom 创建。

Feishu 确认：

```text
“T1001 已恢复”
```

同 canonical user：

```text
RESOLVED → CLOSED
```

---

## AC-19 Resolution Rejected

Requester：

```text
“还没好”
```

结果：

```text
RESOLVED → IN_PROGRESS
```

不创建新 Ticket。

---

## AC-20 Memory After Confirmed Close

只有 CLOSED 后：

```text
MemoryExtractor
```

写长期记忆。

新的 Session：

```text
recall
```

成功。

---

## AC-21 RAG No-answer Real Handoff

RAG 没可靠资料：

如果文案说：

```text
已转人工
```

则必须：

```text
Ticket exists
Operator notification exists
```

---

## AC-22 HITL Execution

```text
ESCALATE T1001
↓
Approval PENDING
↓
APPROVE
↓
Action executed exactly once
```

不能停在 APPROVED。

---

## AC-23 Notification Visibility

Internal note：

```text
Operator sees
Requester does NOT receive
```

---

## AC-24 Notification Dedupe

同一个：

```text
Business Event
+
Audience
+
Target
```

不能重复产生同类 notification。

---

## AC-25 Concurrent Duplicate Webhook

同一个官方 message id：

并发发送。

最终：

```text
1 business execution
1 ticket
0 duplicate ticket
0 500
```

---

## AC-26 Official-shaped Feishu Inbound

使用官方形状 fixture。

Adapter 正确解析：

```text
message_id
open_id
chat_id
chat_type
text
```

---

## AC-27 Feishu Outbound Contract

FakeTransport 收到的请求满足当前官方文档定义：

```text
endpoint
receive_id_type
receive_id
headers
body
```

---

## AC-28 WeCom Official Contract

仅针对当前官方文档明确支持的能力。

Fixture / verifier / outbound contract 必须来源于记录在：

```text
docs/CHANNEL_PROTOCOL_MATRIX.md
```

的官方文档。

---

## AC-29 Full Case Trace

完整 T1001 生命周期可以查询：

```text
inbound
identity
ticket create
notification
claim
resolve
requester confirm
close
memory
```

以及 actor。

---

## AC-30 Existing V1 Regression

原：

```text
AC-01 ~ AC-10
```

全部继续通过。

---

# 64. V2 最终 Demo

必须提供一个本地 Demo / test scenario。

完全不真实联网。

场景：

```text
1. 张三 Requester Group:
   A3 空调坏了

2. canonical identity:
   user_001

3. T1001 created

4. Requester public receipt generated

5. Requester private detail generated

6. Operator work item generated

7. 李师傅:
   canonical user_ops_001
   CLAIM T1001

8. atomic claim succeeds

9. Requester receives lifecycle update

10. Operator RESOLVE T1001

11. Requester confirmation request

12. 张三换另一个 bound channel:
    CONFIRM T1001

13. CLOSED

14. Memory extraction

15. New Session:
    空调又坏了

16. Memory recall
```

另外演示：

```text
FAQ → RAG
```

和：

```text
ESCALATE → Approval → Execute
```

---

# 65. Legacy 只能 ADAPT 思想

可以借鉴：

```text
双 Audience
群简短 / 私聊详情
operator receipt
requester lifecycle notification
requester confirmation
explicit ticket id in shared group
outbound retry thinking
```

禁止 port：

```text
GROUP_CHAT_TO_SYSTEM

十系统

session-centric active_ticket

group:<chat>:user:<sender> 作为身份主模型

TicketAPI 双轨

复合 lifecycle/handoff 状态

metadata pending actions

19 条全部群命令

自定义 HMAC 当官方协议

巨型 wecom_bridge_server

巨大 SupportIntakeWorkflow
```

---

# 66. 结构要求

不要为了完成 V2 又把：

```text
app/main.py
workflow.py
```

写成巨类。

职责必须保持分离。

至少应存在清晰边界：

```text
conversation

roles / authorization

operator actions

ticket

notification

outbound

channel protocol

approval

memory

trace
```

具体目录由你根据当前 V1 结构决定。

不要无意义重构所有已有文件。

---

# 67. 测试要求

必须：

```text
unit
integration
acceptance
concurrency
protocol contract
```

都有覆盖。

---

# 68. 不允许把真实外部服务作为测试依赖

测试：

```text
offline
deterministic
repeatable
```

不得要求：

```text
real token
real corp
real group
real network
```

---

# 69. 数据指标

完成后实际输出：

```text
total pytest passed

V1 regression passed

V2 acceptance passed

RAG Recall@3

Memory eval

concurrent duplicate webhook result

concurrent claim result

notification dedupe result
```

不允许编造。

---

# 70. 文档同步

完成代码后更新：

```text
README.md

docs/PRODUCT_SCOPE.md
docs/ARCHITECTURE.md
docs/DOMAIN_MODEL.md
docs/GOLDEN_PATH.md
docs/ACCEPTANCE_TESTS.md
docs/DEVELOPMENT_PLAN.md
docs/IMPLEMENTATION_BACKLOG.md
docs/LEGACY_PORT_MAP.md
docs/HANDOVER.md
```

并新增：

```text
docs/CHANNEL_PROTOCOL_MATRIX.md

docs/V2_IMPLEMENTATION_REPORT.md
```

---

# 71. CHANNEL_PROTOCOL_MATRIX 必须非常严格

例如：

```text
Feishu
Capability: message receive
Official URL:
Date verified:
Event:
Fields:
Idempotency:
Implemented:
Test fixture:
Real network: disabled
```

WeCom 同样。

禁止只写：

```text
参考官方文档
```

必须记录具体官方页面。

---

# 72. V2_IMPLEMENTATION_REPORT

必须记录：

```text
V1 baseline

schema changes

new domain models

new services

new endpoints

new notification types

channel capability matrix

legacy concepts adapted

legacy code explicitly not ported

concurrency fixes

acceptance results

full pytest result

known limitations

capabilities intentionally disabled pending official spec
```

---

# 73. 不属于 V2 的内容

本次不要引入：

```text
Multi-Agent

MCP

GraphRAG

Kafka

Kubernetes

Redis

Celery

ERPNext

十系统

Telegram

复杂 Web Console

复杂 RBAC

Duplicate Merge

复杂 SLA

复杂 Knowledge Management UI
```

除非现有代码为了 V2 必须极小使用，否则不要增加。

---

# 74. 不以“增加代码量”为目标

如果一项能力可以用：

```text
simple deterministic code
```

完成：

不要引入新框架。

特别不要为了 HITL：

```text
LangGraph suspend/resume
```

除非当前代码存在无法解决的实际必要性。

---

# 75. Implementation Discipline

不要一次写完整仓库然后最后测试。

内部自行按依赖顺序推进。

每完成一个 coherent slice：

```text
run targeted tests
```

关键结构完成后：

```text
run full pytest
```

但是不要暂停等人工确认。

本任务目标是：

> **自主完成 V2 全部范围。**

---

# 76. 遇到官方协议不明确时

执行：

```text
DO NOT GUESS
```

记录：

```text
UNSUPPORTED / PENDING_OFFICIAL_SPEC
```

并确保：

```text
business core still works through simulation
```

不要用第三方文章填补协议缺口。

---

# 77. Definition of Done

只有同时满足以下条件才能宣布 V2 完成：

```text
V1 AC-01~10 all green

V2 AC-11~30 all green

full pytest green

canonical requester identity works

canonical operator identity works

conversation type/purpose exists

requester public/private split works

operator collaboration works

assignment is persisted

concurrent claim safe

concurrent webhook idempotent

notifications use audience/visibility

notification dedupe works

outbox survives simulated delivery failure

official-shaped channel fixtures exist

Feishu protocol contract based on official docs

WeCom implemented capabilities based only on official docs

unsupported channel capabilities explicitly disabled

requester confirmation closes ticket

resolution rejection returns ticket to processing

long-term memory only follows valid closure

HITL approval executes action

case trace covers full lifecycle

no real external credentials required

no external network required
```

---

# 78. 完成后的最终检查

执行：

```bash
git status
git diff --stat
pytest -p no:warnings
```

并检查：

```text
是否破坏 V1 API？
是否出现第二套 Ticket API？
是否重新 session-centric？
是否出现复合 ticket state？
是否把业务状态塞回 metadata？
是否让 Agent 直接执行敏感动作？
是否把 Channel 和 Role 绑定？
是否伪造官方 Channel capability？
是否出现无法解释的通知重复？
是否存在内部信息泄漏到 requester？
```

---

# 79. 最终回复

完成后不要在聊天里复制整个实现报告。

最终只回复：

```text
Support Agent Lite V2 implementation complete.

Branch:
<...>

Commit:
<...>

V1 baseline:
<...>

Full test suite:
<passed / failed>

V1 acceptance:
<...>

V2 acceptance:
<...>

RAG eval:
<...>

Memory eval:
<...>

Concurrent duplicate webhook:
<...>

Concurrent claim:
<...>

Protocol docs verified:
Feishu: <count>
WeCom: <count>

Real external network used:
NO

Real credentials required:
NO

Official capabilities left unsupported/pending:
<list or NONE>

Implementation report:
docs/V2_IMPLEMENTATION_REPORT.md

Protocol evidence:
docs/CHANNEL_PROTOCOL_MATRIX.md

Unexpected modifications:
YES / NO
```

如果测试失败：

不要把任务标为 complete。

继续定位并修复，直到：

```text
full suite green
```

或者存在确实无法解决的外部官方协议阻塞。

如果是官方协议阻塞：

必须清楚记录：

```text
BLOCKED BY OFFICIAL SPEC / CAPABILITY
```

不得自己猜协议来“做完”。

---

这份 Prompt 和我们前面的 V1 Prompt 有一个本质区别：**这次我允许 Codex真正修改领域模型和数据库，但不允许它自由重新设计已经正确的 Core。**

特别是这四条，我建议你不要删：

> **Mock network, not protocol.**
> **Channel != Role.**
> **Shared operator conversation has no implicit active ticket.**
> **Legacy is business evidence, not protocol evidence.**

其中飞书这边已经可以非常严格地做协议 Contract Test：官方目前明确说明 `im.message.receive_v1` 支持单聊和相应权限下的群聊消息，并建议重复推送时按 `message_id` 做幂等；发送消息接口则明确支持 `open_id`、`user_id`、`chat_id` 等接收者类型。([飞书开放平台][1])

企业微信则让 Codex自己在开发时重新查**官方企业微信开发文档**并留下 evidence；如果官方群入站模式找不到官方依据，宁可让 `WeComConnector` 的该 capability 显示 `PENDING_OFFICIAL_SPEC`，也不要把 Legacy 的桥接行为当成官方协议。这会让之后你真正提供企业微信配置/文档时，接入变成**填 Transport/Secret，而不是再重构一次 V3**。

[1]: https://open.feishu.cn/document/server-docs/im-v1/message/events/receive?lang=zh-CN&utm_source=chatgpt.com "接收消息 - 服务端 API - 开发文档 - 飞书开放平台"
