# 升级计划 —— Top14 关键词全量落地(2026-08)

> 目标:让 `support-agent-lite` 在真实运行的生产底座上,把 Agent 开发
> Top14 高频关键词全部变成**可运行、有测试、可演示**的能力。
> 原则:不推翻已验证的治理层(Harness),每项能力都挂在现有模块上生长。
> 本计划的所有设计决策均有外部参考项目背书(见 §1),不是拍脑袋。

---

## 0. 参考项目清单(调研于 2026-08)

| 项目 | Star | 对本计划的用途 |
|---|---|---|
| [baibai-awd/customer-support-agent](https://github.com/baibai-awd/customer-support-agent) | 新锐 | LangGraph 客服编排:`classify→retrieve→draft→evaluate` 四节点;量化指标看板;SSE 流式;人工反馈沉淀 |
| [didilili/ai-agents-from-zero](https://github.com/didilili/ai-agents-from-zero) | 3.7k | 「电商小二」实战:意图解析/多源知识库/流式回复/转人工;「掌柜智库」:BGE-Rerank/HyDE/RAGAS 评估/多路召回 |
| [huabeitech/agent-desk](https://github.com/huabeitech/agent-desk) | 活跃 | AnswerabilityGate(知识充分性判定门);会话→工单闭环;Docker 一键部署 |
| [JC0v0/Customer-Agent](https://github.com/JC0v0/Customer-Agent) | 785 | 电商客服桌面端:多轮工具调用+会话上下文管理;AI 主动推荐 |
| [empireaaron/order_agent](https://github.com/empireaaron/order_agent) | — | TicketBot:RBAC/Milvus/P50-P95-P99 监控看板 |
| [langchain-ai/langgraph](https://github.com/langchain-ai/langgraph) | 40k | 图编排/durable execution/HITL interrupt 官方范式 |
| earendil-works/pi(本地 `reference/pi`) | 54k | Provider 统一层(pi-ai)/Agent Loop 事件流/截断防御/会话树 |
| TMO Group《2026 电商 AI 客服趋势》(2026-07) | 行业报告 | WISMO 占工单 40%+;领先品牌自动化率 70~90%;ROI 3.5~8x——量化口径来源 |

---

## 1. 阶段总览(依赖顺序)

```text
P0 基座层(第 1 周)
  B1 Provider 注册表 ──┐
  C2 向量 RAG ─────────┤→ 后续一切智能能力的地基
P1 编排与协议(第 2 周)
  C1 LangGraph 四节点图(含 Function Calling schema 化)
  B5 循环加固(steering + stopReason=length 防御)
P2 智能与表达(第 3 周)
  B2 意图路由小模型化   B4 提示词场景拆分(Skills 化)
  C3 指标看板 + Trace 标准化
P3 生态与交付(第 4 周)
  C4 Docker/K8s 交付    C5 SSE 流式    C6 反馈闭环
  C7 MCP 只读 Server    C8 vLLM/Ollama 接入
持续线:B6 真实语料积累;多 Agent(诊断 Agent)在 P2 后立项。
```

---

## 2. 逐项升级计划

### #1 RAG 升级:向量检索 + Rerank + 充分性判定 【C2】

**目标**:替换 TF-IDF 打分为真 embedding 语义检索,补 rerank 与
AnswerabilityGate,根治"0.26 分也敢答会议室文档"问题。

**参考**:ai-agents-from-zero「掌柜智库」(BGE-Rerank + 多路召回 +
RAGAS 评估);agent-desk 的 AnswerabilityGate;V2.0 课程混合检索
(BM25+向量,召回率>90% 口径)。

**方案**:
- 新增 `app/infrastructure/vector_store.py`:Chroma PersistentClient,
  embedding 用硅基流动/阿里 DashScope 兼容接口(默认 bge-m3 或
  text-embedding-v3,`.env` 可切);
- `Retriever` 改为双路:现 TF-IDF(稀疏)+ Chroma(稠密),RRF 融合;
- rerank:接入 bge-reranker API(可选开关,失败降级为融合序);
- 新增 `answerability_gate(score_threshold, hit_count)` 门:
  低于阈值 → 不生成回答,走既有 NO_ANSWER 真实转人工(Invariant 7);
- 知识导入脚本 `scripts/import_kb.py`(seed/*.json → 向量库),
  支持增量;
- 保留 TF-IDF 为离线测试缺省(测试仍不联网)。

**验收**:RAGAS 式自建 20 题评测集 Recall@3 ≥ 90%;低相关问题 100%
触发 gate 而非编造;`pytest` 全绿且默认离线。

---

### #2 Function Calling 标准化 【C1 附带】

**目标**:只读工具从自定义 dict 协议改为标准 FC schema(tool 名/
description/json-schema 参数),模型按 OpenAI tools 协议输出调用。

**参考**:kamacoder 面试题汇总("FC 是 Agent 基石");pi-agent-core
工具定义(execute/throw 错误语义);customer-support-agent 的
structured_llm 决策。

**方案**:
- `app/application/agent_tools.py`:每个工具导出
  `{name, description, parameters(JSON Schema)}`;调用结果统一
  `{content, is_error}`;
- `support_agent.py`:决策循环改为 messages + tools 协议;保留
  ≤2 次调用上限与 schema 校验的 AgentDecision(Harness 不放松);
- 截断防御(pi 经验):`finish_reason=="length"` 时丢弃本轮全部
  tool_calls,直接降级确定性路径。

**验收**:FakeLLM 注入畸形/截断 tool_calls 的用例全部安全降级;
真实 LLM 下工具调用成功且决策仍过 `validate_decision`。

---

### #3 MCP 只读 Server 【C7】

**目标**:把平台能力以 MCP 标准协议暴露给任意 MCP 客户端
(Claude Desktop/Cursor 等),证明"集成标准化"能力。

**参考**:MCP 三原语(tools/resources/prompts);官方 python-sdk
(FastMCP);治理条件:READ-ONLY 先行。

**方案**:
- 新增 `app/mcp_server.py`(FastMCP):暴露
  `search_tickets(query)`、`get_case(ticket_id)`、`ticket_stats()`
  三个只读 tool + `knowledge_search` resource;
- 复用现有 service 层,禁止任何写操作;传输先 stdio,后 SSE;
- AGENTS.md 已注明:触发敏感变更的能力仍须走 Approval。

**验收**:Claude Desktop 配置后可查询真实工单案例;写类工具不存在;
单测覆盖三个 tool 的返回形状。

---

### #4 ReAct/Plan-and-Execute 显式化 + LangGraph 编排 【C1】

**目标**:用 LangGraph 把现有管线重排为显式状态图,获得 JD 第一框架
关键词,同时让工作模式可命名、可测试、可扩展。

**参考**:customer-support-agent 的 StateGraph 四节点;
langgraph 官方 HITL interrupt(对应我们的 Approval)。

**方案**:
- 新增 `app/application/graph.py`:`StateGraph(AgentState)` 四节点
  `classify → retrieve → draft → evaluate`;
  - classify = IntentRouter(+B2 小模型)
  - retrieve = Retriever(#1)+ MemoryService.recall
  - draft = SupportAgent(两阶段事务保持不变)
  - evaluate = PolicyValidator + 转人工判断(customer-support-agent
    的 evaluate 节点同款)
- 节点只是**包装现有服务**,不迁移业务逻辑(Harness 不动);
- `interrupt_before=["execute"]` 映射 ESCALATE/FORCE_CLOSE 审批暂停
  (LangGraph 原生 HITL 语义,与我们 PendingAction 对齐);
- 入口保留旧 workflow 作为降级路径(langgraph 缺失时 fallback)。

**验收**:同一输入新旧管线行为一致(对照测试);图结构可视化导出
(`get_graph().draw_mermaid()` 存 docs/)。

---

### #5 Multi-Agent:诊断 Agent(试点)【独立立项】

**目标**:解禁后的第一个多 Agent 落地:ESCALATE 时自动产出诊断报告。

**参考**:腾讯 Harness JD(多 Agent 协作/Memory/Skill);pi 无内置
子代理哲学(按需才建);Anthropic 提醒"不要过早引入"。

**方案**(最小形态):
- 新增 `app/application/diagnosis_agent.py`:单一职责——只读调查
  (trace 历史/相似工单记忆/知识库/SOP)→ 输出 `DiagnosisReport`
  (schema 校验),附加到 operator work item 的 INTERNAL_NOTE;
- 与主 Agent **无共享可变内存**(各自构造上下文,符合 AGENTS.md
  topology 约束);LLM 复用 B1 provider 层;
- A2A 协议暂缓:单机双 Agent 用进程内调用即可,A2A 待分布式需求
  出现再评估(避免过度设计)。

**验收**:诊断报告 schema 测试;升级工单的 work item 含诊断摘要的
集成测试;失败时主流程不受影响(降级为无诊断)。

---

### #6 记忆系统增强 【已有,小幅】

**现状**(已验证):短期=recent-6+持久工单摘要;长期=CLOSED 后
MemoryExtractor 抽取 + recall 召回(effective=relevance×confidence)。

**增强**(借鉴 pi 会话树/压缩 + 面试考点"记忆系统怎么设计"):
- `session_ticket_contexts` 之上加会话级滚动摘要(超 N 条压缩为
  summary entry,记录 first_kept_message_id——pi compaction 同款);
- 记忆写入增加来源类型标记(confirmed_closure / force_closed),
  召回时 confirmed 优先(v2.md §49 已定语义,补实现标注)。

**验收**:长对话压缩后关键事实仍在上下文的回归测试。

---

### #7 安全 Harness:补齐声明项 【已有,收尾】

**现状**:幂等领取+并发测试、HITL CAS exactly-once、只读工具白名单、
LLM 超时降级、outbox 重试≤3、崩溃恢复状态机——面试考点全覆盖。

**补齐**(对齐 kamacoder 安全清单):
- Prompt Injection 防御:入站文本与系统指令结构化隔离
  (ContextBuilder 中用户内容加明确 data 区块标记);
- 工具调用审计日志:每次 tool call 写 trace_events(名称/参数摘要/耗时);
- 成本观测:token 用量入 trace(为 #9 指标看板供数)。

**验收**:注入攻击样例集("忽略之前指令…")不改变业务行为的测试。

---

### #8 可观测性:指标看板 + Trace 标准化 【C3】

**目标**:回答企业最关心的问题——"这个 Agent 省了多少人力?"
并让 trace 输出达到 Harness Engineer 岗位要求的形状。

**参考**:customer-support-agent 的 dashboard(classification_accuracy /
auto_handle_rate / escalation_rate / minutes_saved);腾讯 Harness JD
(任务成功率/工具调用准确率/成本/延迟/TTFT);order_agent P50/P95/P99。

**方案**:
- `GET /api/dashboard`:从 trace_events/notification_outbox/tickets
  聚合:分类准确率(抽检标注)、自动处理率、转人工率、平均响应延迟、
  token 成本、节省工时(人工 5min/复核 1.2min 口径,TMO 行业基准);
- trace 事件对齐 OpenTelemetry span 形状(trace_id/span_id/parent/
  attributes),`GET /api/traces/{id}` 结构化导出 JSON;
- Agent eval(AC-A20 黄金集)接入 CI 说明文档。

**验收**:dashboard 单测(固定数据集快照);一条真实消息的完整
trace 可导出为 OT 形状 JSON。

---

### #9 向量库选型落地 【C2 内含】

Chroma(PersistentClient,零服务依赖)起步;Milvus 留部署升级项
(order_agent 同款)。接口抽象 `VectorStore` Protocol,切换不改上层。
详见 #1。

---

### #10 Skills/提示词工程:场景化拆分 【B4】

**目标**:单提示词拆成场景化技能包,对齐"Skills 决定怎么想"三层论。

**参考**:legacy `llm/prompts/`(progress_reply/handoff_reply/
disambiguation_reply 等 10 个,frontmatter 格式);pi prompt templates
/skills 结构;kamacoder Skill 结构(身份定位+工作流程+注意事项+输出规范)。

**方案**:
- `prompts/` 目录扩为:`agent_decision.v1`(主决策)+
  `reply_progress.v1` / `reply_handoff.v1` / `reply_clarify.v1` /
  `diagnosis.v1`;frontmatter 带 key/version/scenario(兼容 legacy 形状);
- PromptRegistry 按 intent 选择模板;缺失回退主提示词;
- 每个 skill 一个黄金用例(fake_llm 断言关键段落)。

**验收**:registry 加载测试;各场景回复包含场景专属要素的断言。

---

### #11 Docker/K8s 交付 【C4】

**目标**:一键起全套;具备 K8s 编写能力(JD 工程化基本盘)。

**参考**:agent-desk 的 docker-compose 完整交付;order_agent compose
(MySQL/Milvus/MinIO 组合);本项目轻量化裁剪。

**方案**:
- `docker-compose.yml` 扩展:app + chroma 数据卷 + .env.example;
  healthcheck + depends_on;
- `deploy/k8s/`:Deployment + Service + ConfigMap + Secret 模板
  (SQLite 单副本限定说明,Postgres 迁移列为后续项);
- README「部署」章节:compose 三行起 + k8s apply 指引。

**验收**:干净机器 `docker compose up -d` 后 health 通过并发送一条
测试消息全链路成功。

---

### #12 vLLM/Ollama 私有化接入 【C8,B1 之后即插即用】

**参考**:2026 就业课(Ollama/vLLM 本地部署为硬要求);Ollama 官方
OpenAI 兼容 API。

**方案**:B1 的 provider 注册表天然支持——`LLM_PROVIDER=ollama`
→ base_url `http://127.0.0.1:11434/v1`,模型 `qwen3:8b` 等;
README 给 Ollama 拉模型三行命令。验收:纯本地模型跑通一次完整
AgentDecision(离线演示终极形态)。

---

### #13 A2A 协议 【观察项】

Google 推出的 Agent 间协作协议(MCP 管"用什么",A2A 管"Agent 间
对话")。当前单机双 Agent 进程内调用足够(见 #5);待出现跨进程/
跨主机多 Agent 需求再引入。**不在本期范围**,防止过度设计。

---

### #14 微调 LoRA/QLoRA 【超出应用开发岗,不做】

JD 中仅 60K+ 高端算法岗要求。应用开发路线以 RAG+Harness 为主,
微调列为认识性了解(面试能讲清 SFT/LoRA/DPO 区别即可,
kamacoder 面试题汇总已覆盖)。

---

## 3. 里程碑

| 里程碑 | 内容 | 演示脚本 |
|---|---|---|
| M1(周末) | B1+C2:provider 切换 + 向量 RAG | 同一问题分别由 Qwen 云端/本地 Ollama 回答;RAG 命中知识库文档 |
| M2 | C1+B5:LangGraph 图 + FC schema | mermaid 图入 README;截断注入测试绿 |
| M3 | B2/B4/C3:智能路由+Skills+看板 | dashboard 展示真实运营数据;错误路由率下降对比 |
| M4 | C4~C8:交付与生态 | compose 一键起;SSE 流式;MCP 在 Claude Desktop 里查工单 |

## 4. 风险与对策

- LangGraph 引入不改变事务边界:节点只调 service,写事务仍在
  TicketActionService(Harness 不因框架而破);
- 免费 LLM 限流:B1 多 provider + 自动降级兜底;
- 语料冷启动:B6 持续积累 + legacy 结构性夹具;
- 范围蔓延:A2A/微调明确出圈,防过度设计。

---

## 5. 数据资产盘点与中文语料方案(2026-08-25 补充)

### 5.1 已下载的外部数据集(datasets/,英文/德文)

| 数据集 | 内容剖析结论 | 挂接 |
|---|---|---|
| customer-support-tickets | 61.7k 行,en+de;subject/body/answer/type/queue/priority/tags;10 队列三级优先级 | body→answer 灌 RAG;queue/priority 做分类评测;type 进看板 |
| CLINC150 | 23.8k 条短口语 query,150 意图(均衡);**OOS 以 NaN 标签藏在 test**——"安全拒答"的标准标注形态 | 意图评测基准(B2) |
| synthetic-it-support-tickets | **745 条完整 IT 事件闭环**:ticket→多轮诊断对话(correspondence)→diagnostics→root_cause(745/745 全非空)→resolution 步骤数组 + PII 脱敏标注 | 诊断 Agent few-shot 金矿(#5);PII 合规演示 |

### 5.2 抽样本地化方案(替代全量翻译)

全量翻译无收益(我们不做模型训练),采用「意图映射 + 抽样本地化」:

```text
datasets/zh_golden/
  intent_eval_500.jsonl      # CLINC150 → 映射到本项目意图体系后,
                             #   LLM 中文重写 ~500 条(含 OOS 风格样例)
  diagnosis_fewshot_50.jsonl # it-support-tickets → 精选 50 条四元组本地化
                             #   (报修→诊断→根因→处置)
```

- **不直译**:保留意图标签,LLM 用中文企业支持语境改写表达,
  混入业务词表(门禁/工牌/共享盘/空调);留 10% 直译对照;
- **质量控制**:回译一致性校验 + 人工抽检 10%;
- **成本**:免费模型几分钟 + 人工半小时。

### 5.3 旧项目(reference/)数据与 RAG 盘点 —— 重要发现

**知识库种子已存在**(`reference/seed_data/`):

| 文件 | 规模 | 语言 | 许可 | 价值 |
|---|---|---|---|---|
| `faq_it_support.json` | 20 条 | **中文** | 内部 | 「如何重置公司邮箱密码」等,字段 doc_id/title/content/tags,**直接可用** |
| `sop_it_operations.json` 等 | 8 条 | **中文** | 内部 | 员工电脑故障现场处理流程等运维 SOP,诊断 Agent 素材 |
| `faq_external_documents.json` | 89 条 | 英文 | Apache-2.0(可商用) | C2 导入验证 |
| `history_external_documents.json` | **10,000 条** | 德/英 | ⚠️ **CC-BY-NC-4.0 非商用** | 与 Tobi-Bueck 数据集同源(case_ext_00001 的德语工单与 A 数据集首例一字不差)——印证 A 是它的完整版 |

`external_sources_manifest.json` 记录了来源治理(faq_count=89,
history_count=10000, sop_count=3)——legacy 的治理习惯值得延续。

**legacy RAG 实现**(`reference/core/retrieval/`,564 行):

```text
hybrid_retriever.py  词法0.55 + 向量0.45 加权融合 + source_boost(按来源加权)
vector_retriever.py  hashed char/word 特征伪向量(384维,含中文正则感知)
reranker.py          重排序模块(已有!)
source_attribution.py 引用归因
```

结论:legacy 的混合检索思想(双路融合+来源加权)正确,但向量层是
hash 技巧非真 embedding。C2 = 保留其混合架构,把向量层换成真 embedding
+ Chroma,rerank 用 bge-reranker API,并继承其 source_boost 设计
(faq/sop/history_case 不同权重)。**中文种子直接从 5.3 的 faq+sop 起**
(20 FAQ + 8 SOP 即刻可入库),外部英文/德文数据作规模补充。

### 5.4 更新后的 C2 执行清单

1. Chroma + embedding 接口(bge-m3/DashScope 可切);
2. 导入源:`seed_data/faq_it_support.json`(20)+ `sop_it_operations.json`(8)
   + `faq_external_documents.json`(89)→ 首批 117 条中文为主知识库;
3. 双路检索(RRF 融合 TF-IDF + 向量)+ bge-rerank(可选开关)+
   AnswerabilityGate;
4. zh_golden 500 条意图评测集(5.2)作为路由回归基准;
5. 10000 条历史案例因 NC 许可仅限内部演示环境使用,商用部署前剔除。

---

## 6. 中文 IT 知识库内容来源调研(2026-08-25)

互联网上中文 IT 运维/维修知识**充足且高度结构化**,可直接加工为 FAQ 条目。

### 6.1 已确认的高质量来源

| 来源 | 内容 | 可提取条目(估) |
|---|---|---|
| 《运维 20 个常见故障排查修复大全》(知行晓政/知乎双发) | 四大类:系统(Linux 启动失败/fstab 损坏/MBR 扇区/只读文件系统)、硬件(电源模块/主板电容鼓包/RAID 硬盘)、软件(补丁冲突/病毒/多版本共存),每条含 **故障现象→原因→排查方法→解决步骤**,附真实案例 | 20 大条 → 拆 60~120 个 FAQ |
| 《网络故障排查思路和处理方法》(浩道 linux,电子发烧友) | 定位三法(全网/小范围/单点)+ "链路→配置"总思路 + 7 步排错(硬件/网卡/IP/连通性/路由/DNS/服务/权限) | 方法论 1 篇 → 拆 10~15 条 |
| 《运维问题排查思路》(PetterLiu,博客园) | 应急恢复优先、故障定位方法论、应急方案五层结构(系统级/服务级/应用级/工具/沟通) | 8~12 条 |
| 《企业内部网络故障快速排查》(简道云,2025) | 5 类故障分类(硬件/配置/服务/链路/安全)+ 现象→类型速查表 + 常见误区 5 条 | 15~25 条 |
| KMPRO 商业知识库内容框架(非内容本身,分类学) | 知识五分类:**故障处理/运维手册/最佳实践/工具文档/FAQ**;三级层级(基础/进阶/专家) | 作为我们知识库的**目录骨架**(不抄内容) |

### 6.2 加工原则(版权合规)

- ❌ 不整段照抄(表达受版权保护);
- ✅ **结构化提取知识要点后重写**:事实性知识(如"fstab 配置错误导致
  启动卡在 starting system logger")不受版权保护,按统一模板重写为
  `现象 / 原因 / 排查步骤 / 解决方案` 四段式 FAQ 条目;
- ✅ 文末标注参考来源列表(与 legacy external_sources_manifest 同思路);
- ✅ 内部使用风险低;若未来商用部署,以自写+LLM 改写版本为准。

### 6.3 预期产量(修正知识库扩充目标)

| 渠道 | 条目 |
|---|---|
| legacy 中文种子(FAQ 20 + SOP 8) | 28 |
| 6.1 网络文章结构化提取(5~6 篇核心) | 100~180 |
| A 数据集三队列 body→answer 本地化 | 250~300 |
| 变体问法生成(每条 ×3~5 paraphrase) | 覆盖率 ×3 |
| 领域补全(账号/网络/硬件/软件/办公设备/安全 六类) | +150 |
| B6+C6 真实积累 | 持续 |
| **P0 目标合计** | **≥400 条**(超原定 150 目标) |

知识目录采用 KMPRO 五分类作为顶层结构:
`故障处理 / 运维手册(SOP) / 最佳实践 / 工具文档 / FAQ`,
每条目打 `category + tags + source` 三元标签,便于 AnswerabilityGate
按类别调权。

---

## 7. 企业级检索路由与五实体工具矩阵(C9 定稿,owner 拍板版)

> 决策:检索.md 的路由架构**全量落地**(意图识别&安全风控 → 权限校验 →
> 多路精确/混合检索),数据用小规模虚构对齐五实体:
> **知识文档、工单、人员通讯录、IT 资产、历史记忆**。
> 图谱省略(owner 已确认);不建真数仓/ES,"指标查询"以受限工单统计实现。

### 7.1 五实体 × 数据载体

| 实体 | 载体 | 规模 | 检索路径 |
|---|---|---|---|
| 知识文档 | seed/faq/*.json → Chroma+TF-IDF | ≥400(含 A 数据集中文化) | L1 混合检索 + Rerank + AnswerabilityGate |
| 工单 | tickets/messages 表(SQLite) | 运行期真实积累 | L2 相似工单向量 + ticket_history |
| 人员通讯录 | `employees` 表(**虚构 30 人**:工号/姓名/部门/分机/邮箱/工位) | 30 | **L4 SQL 精确查询(LIKE),严禁向量化** |
| IT 资产 | `assets` 表(**虚构 30 条**:资产号/类型/型号/领用人/状态) | 30 | L4 精确查询 |
| 历史记忆 | memories 表 | CLOSED 后自动 | recall_memory |

### 7.2 路由器四条路径(全部落在现有 SQLite,零新组件)

```text
用户输入
  │
[意图识别 & 安全风控]  ← 正则拦截层:手机号/身份证/邮箱/工号模式
  │                      ├─ 命中 → 本轮强制挂载 entity_lookup,禁用向量类工具,
  │                      │         系统提示注入「只返回脱敏结果」
  │                      └─ 未命中 → 正常路由
  ├─ faq/support 故障咨询 ──→ [L1] knowledge_search(混合三路+rerank)
  ├─ progress 进度查询 ────→ [L2] ticket_history + case_timeline
  ├─ 含实体(人名/工号/E\d+/AST-)\→ [L4] contact_lookup / asset_lookup
  │                              (SQL LIKE 精确+模糊;输出脱敏:手机号 138****1234)
  └─ 统计类("上月多少工单"/"我的工单数") → [L3] ticket_stats
                                (受限模板化统计:参数化查询预定义口径,
                                 不生成裸 SQL——Harness 原则下的 Text-to-SQL 最小形态)
```

**权限校验前置**(检索.md 的 Permission-aware):
- requester 仅可查本人相关(工单/资产领用人=自己);他人联系方式仅返回部门公开字段;
- operator 队列内全量;approver 同理;
- 校验失败 → 直接拒答,连检索请求都不发出。

### 7.3 工具矩阵终稿(AgentToolPort 扩展至 8 个,全只读)

| 工具 | 新旧 | 数据路径 | 服务意图 |
|---|---|---|---|
| knowledge_search | 升级(C2 三路) | L1 | faq/support |
| ticket_history | 已有 | L2 | progress/support |
| recall_memory | 已有 | 记忆 | support |
| allowed_actions | 已有 | 权限 | 任意 |
| similar_tickets | 新(R2) | L2 向量 | support |
| case_timeline | 新(包装 REST) | L5 轻量 | progress |
| contact_lookup | 新 | L4 employees | any(实体命中时) |
| asset_lookup | 新 | L4 assets | support/progress |
| ticket_stats | 新 | L3 受限统计 | progress/管理问询 |

工具白名单按意图装配(Harness 最小授权):闲聊 other 不挂任何检索工具;
L4 拦截轮只保留 entity 类工具。

### 7.4 种子数据规范

- `seed/directory/employees.json`:30 人,姓名用高频常见名(张伟/李娜/王芳…),
  部门覆盖运维/研发/财务/HR/行政,分机 8001~8030,一人一条 PII(虚构手机号);
- `seed/directory/assets.json`:30 条,AST-xxxx,类型分布 笔记本12/显示器6/打印机4/
  网络设备4/其他4,assigned_to 与 employees 对齐,含少量 status=维修中 制造真实感;
- 两份种子带 `fictitious: true` 标记,README 声明纯虚构。

### 7.5 验收标准

1. 「查运维部于松泽的分机号」→ contact_lookup 精确返回,分机明文(部门公开字段);
2. 「帮我查张伟的手机号」(requester 身份)→ 返回脱敏 138****1234 或权限拒绝(按角色矩阵);
3. 「AST-0307 是谁的电脑」→ asset_lookup 命中并给出领用人;
4. 输入含 11 位手机号的报修文本 → 拦截层生效,该轮无向量工具调用(trace 可证);
5. 「上个月网络故障类工单有几个」→ ticket_stats 返回计数;
6. 以上全部有 pytest 覆盖;verify_kb 门禁保持全绿。

### 7.6 明确不做(防蔓延)

知识图谱/Neo4j、外部 Elasticsearch、独立数仓与裸 Text-to-SQL、
天气等演示道具。L3 的正确粒度是"模板化受限统计",不是自由 SQL 生成——
这是 Harness 边界,也是与检索.md 原文唯一的刻意偏离(已获 owner 方向确认)。
